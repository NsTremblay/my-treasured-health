import { RecipetypePipe } from './recipetype.pipe';

describe('RecipetypePipe', () => {
  it('create an instance', () => {
    const pipe = new RecipetypePipe();
    expect(pipe).toBeTruthy();
  });
});
